apiVersion: v1
kind: Service
metadata:
  name: {{ include "graphql-taskservice.fullname" . }}
  labels:
    {{- include "graphql-taskservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "graphql-taskservice.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
